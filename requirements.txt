# Core (schemas, configs)
pydantic>=2.0,<3
PyYAML>=6.0
pandas>=2.0,<3
openpyxl>=3.1,<4

# Dev / tests
pytest>=7.0

# Windows-only dependency for reading .doc via Word automation.
pywin32>=306

# Optional: add when implementing pipelines (uncomment as needed)
# openpyxl>=3.1  # document_manifest.xlsx
# numpy>=1.24
# torch>=2.0
