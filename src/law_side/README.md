# `law_side`

Corpus preprocessing, segmentation, normative sentence detection, legal frames, predicate normalization, rule building/validation/export. Outputs feed `data/interim/law_parsing/` and `data/processed/rulebase/`.
