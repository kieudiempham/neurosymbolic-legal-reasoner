# Experiments (draft)

We report on datasets under `data/processed/datasets/`, baselines, and metrics implemented in `src/evaluation/`. Tables and figures live under `experiments/tables/` and are referenced from the main text.
