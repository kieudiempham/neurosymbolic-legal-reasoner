# Method (draft)

Subsections follow the codebase layout: question parsing (`question_side`), statute processing (`law_side`), retrieval, backward/forward reasoning, verification, then answer generation.
